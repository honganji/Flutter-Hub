import 'package:flutter/material.dart';

class DesktopSettingScreen extends StatelessWidget {
  const DesktopSettingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Desktop Setting'),
    );
  }
}
