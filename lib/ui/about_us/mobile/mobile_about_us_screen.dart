import 'package:flutter/material.dart';

class MobileAboutUsScreen extends StatelessWidget {
  const MobileAboutUsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Mobile About Us'),
    );
  }
}
